<Page
    x:Class="App8.MovieFrontPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <local:ViewModelMovie></local:ViewModelMovie>
    </Page.DataContext>
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">


        <StackPanel Width="Auto" Height="200" HorizontalAlignment="Center" VerticalAlignment="Top" Background="Red" >
            <ListView ItemsSource="{Binding Movies, Mode=TwoWay}">
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <!-- 'styler' elementer i listen -->
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding title}"></TextBlock>
                            <TextBlock Text="{Binding room}"></TextBlock>
                            <TextBlock Text="{Binding Time}"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>


    </Grid>
</Page>
